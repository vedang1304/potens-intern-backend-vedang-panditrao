# Profile-to-Recommendation API

A backend REST API built using **Node.js, Express.js, PostgreSQL, and Prisma ORM** that recommends the top three job opportunities based on a candidate's profile.

This project was developed as part of the **Potens Backend Internship Assignment**.

---

# Features

## Core Features

- Profile-based recommendation engine
- Returns top 3 ranked job recommendations
- Detailed reasoning for every recommendation
- Complete CRUD operations for job catalogue
- Admin authentication using API token
- Eligibility explanation endpoint
- PostgreSQL database with Prisma ORM
- Seed script containing 15+ job listings
- Input validation
- Unit tests

---

## Stretch Features Implemented

- Recommendation caching using Node Cache
- Swagger / OpenAPI documentation
- Docker & Docker Compose support

---

# Tech Stack

- Node.js
- Express.js
- PostgreSQL
- Prisma ORM
- Swagger UI
- Node Cache
- Jest
- Supertest
- Docker
- Docker Compose

---

# Project Structure

```
├── prisma
│   ├── migrations
│   ├── schema.prisma
│   └── seed.js
│
├── src
│   ├── config
│   ├── controllers
│   ├── middlewares
│   ├── routes
│   ├── services
│   ├── utils
│   ├── validators
│   ├── app.js
│   └── server.js
│
├── tests
│
├── Dockerfile
├── docker-compose.yml
├── package.json
└── README.md
```

---

# Recommendation Logic

Each recommendation is generated by evaluating multiple profile attributes.

The matching score considers:

- Technical Skills
- Years of Experience
- Educational Qualification
- Preferred Job Category
- Preferred Work Mode
- Preferred Location
- Minimum Salary Expectation

Jobs are ranked based on their total score and the top three recommendations are returned along with a human-readable explanation describing why each job matches the candidate profile.

---

# API Endpoints

## Recommendation

### POST /recommend

Returns the top three matching jobs.

Example Request

```json
{
    "skills":["Java","Spring Boot","SQL"],
    "experience":2,
    "education":"BTech",
    "preferredLocation":"Pune",
    "preferredWorkMode":"HYBRID",
    "preferredCategory":"Backend",
    "minimumSalary":1000000
}
```

---

## Job Catalogue

### GET /items

Returns all jobs.

Requires Admin Token.

---

### POST /items

Create a new job.

Requires Admin Token.

---

### PUT /items/:id

Update a job.

Requires Admin Token.

---

### DELETE /items/:id

Delete a job.

Requires Admin Token.

---

## Eligibility Explanation

### GET /explain/:item_id

Returns the eligibility criteria for a specific job.

Example Response

```json
{
    "success": true,
    "data": {
        "jobId": 2,
        "title": "Backend Developer",
        "eligibility": "Candidates should have 1-3 years of experience, a BTech degree, and skills in Java, Spring Boot and SQL."
    }
}
```

---

## API Documentation

Swagger UI is available at

```
http://localhost:5000/api-docs
```

---

# Authentication

Administrative endpoints are protected using a simple API key.

Header

```
x-admin-token: potens_admin_123
```

---

# Installation

## Clone Repository

```bash
git clone <repository-url>

cd potens-intern-backend-vedang-panditrao
```

---

## Install Dependencies

```bash
npm install
```

---

## Environment Variables

Create a `.env` file.

```env
DATABASE_URL="postgresql://postgres:<password>@localhost:5432/recommendation_api?schema=public"

PORT=5000

ADMIN_TOKEN=potens_admin_123
```

---

## Run Database Migration

```bash
npx prisma migrate dev
```

---

## Seed Database

```bash
npx prisma db seed
```

---

## Start Server

Development

```bash
npm run dev
```

Production

```bash
npm start
```

---

# Running with Docker

Build and start the application

```bash
docker compose up --build
```

The application will be available at

```
http://localhost:5000
```

Swagger documentation

```
http://localhost:5000/api-docs
```

---

# Running Tests

```bash
npm test
```

The project includes unit tests covering:

- Missing required fields
- Successful recommendations
- No recommendation scenario
- Eligibility explanation endpoint
- Boundary validations

---

# Design Decisions

### Prisma ORM

Prisma was selected for its type safety, migration support, and simplified database access.

### PostgreSQL

PostgreSQL provides reliable relational storage suitable for structured recommendation data.

### Recommendation Scoring

Instead of strict filtering, a weighted scoring algorithm was implemented. This allows partially matching jobs to still be recommended if they satisfy important criteria.

### Caching

Recommendation responses are cached using Node Cache to improve performance for repeated requests with identical profiles.

### Swagger

Swagger was added to provide interactive API documentation for easier testing and integration.

### Docker

Docker Compose enables one-command setup of both the application and PostgreSQL database.

---

# Future Improvements

If additional development time were available, I would implement:

- Real authentication using JWT
- Role-based authorization
- Webhook subscription for newly matching jobs
- Recommendation history
- Recommendation analytics
- Redis caching
- CI/CD pipeline with GitHub Actions
- Pagination and filtering for job catalogue
- Advanced recommendation algorithm using weighted machine learning models

---

# Known Limitations

- Admin authentication currently uses a static API token.
- Recommendation scoring uses manually defined weights.
- Cache is stored in memory and resets when the server restarts.

---

# AI Use Log

| Tool | Approximate Usage | Purpose |
|------|-------------------|---------|
| ChatGPT | ~150 messages | API design, debugging, recommendation logic, Prisma, Docker, testing, documentation, README |
| Prisma Documentation | Multiple references | ORM configuration, migrations and seed setup |
| Express Documentation | Multiple references | Middleware and routing |
| Swagger Documentation | Multiple references | OpenAPI configuration |
| Docker Documentation | Multiple references | Dockerfile and Docker Compose setup |

AI assistance was used to accelerate development, debug issues, understand framework configurations, and improve documentation. All code was reviewed, integrated, tested, and validated before submission.

---

# Author

**Vedang Panditrao**

Backend Internship Assignment Submission